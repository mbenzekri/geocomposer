{
  "name": "geocomposer-db",
  "private": true,
  "scripts": {
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "shell": "docker exec -it geocomposer-postgis psql -U postgres -d postgres",
    "dump": "bash scripts/dump.sh",
    "restore": "bash scripts/restore.sh",
    "reset": "docker compose down -v && rm -rf data && docker compose up -d && bash scripts/restore.sh"
  }
}