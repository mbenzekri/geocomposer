{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://geocomposer.local/config-and-rules.schema.json",
  "title": "GeoComposer config with expansion rules",
  "description": "Schema éditeur autorisant la configuration GeoComposer avant expansion des $defs, $ref locaux et variables d’environnement",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "services",
    "sources",
    "layers"
  ],
  "properties": {
    "$schema": {
      "anyOf": [
        {
          "type": "string",
          "description": "JSON Schema URI used by editors for validation and autocompletion. Usually keep this value unchanged."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
        }
      ],
      "description": "JSON Schema URI used by editors for validation and autocompletion. Usually keep this value unchanged."
    },
    "server": {
      "anyOf": [
        {
          "$ref": "#/$defs/server",
          "description": "Optional HTTP server and logging configuration."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "port": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 65535,
                  "description": "HTTP server port. Minimum: 0. Maximum: 65535."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "HTTP server port. Minimum: 0. Maximum: 65535."
            },
            "logLevel": {
              "anyOf": [
                {
                  "type": "string",
                  "enum": [
                    "DEBUG",
                    "LOG",
                    "WARN",
                    "ERROR",
                    "NONE"
                  ],
                  "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Optional HTTP server and logging configuration."
    },
    "services": {
      "anyOf": [
        {
          "$ref": "#/$defs/services",
          "description": "Enabled service endpoints. Required."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "wms": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmsService",
                  "description": "WMS service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "maxWidth": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image width in pixels. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum WMS map image width in pixels. Minimum: 1."
                    },
                    "maxHeight": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image height in pixels. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum WMS map image height in pixels. Minimum: 1."
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "WMS service configuration."
            },
            "api": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ogcFeaturesService",
                  "description": "OGC API Features service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "defaultLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                    },
                    "maxLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "OGC API Features service configuration."
            },
            "xyz": {
              "anyOf": [
                {
                  "$ref": "#/$defs/xyzService",
                  "description": "XYZ tile service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "maxScaleFactor": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "XYZ tile service configuration."
            },
            "wmts": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmtsService",
                  "description": "WMTS service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "WMTS service configuration."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Enabled service endpoints. Required."
    },
    "tilesets": {
      "anyOf": [
        {
          "$ref": "#/$defs/tilesets",
          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
    },
    "projections": {
      "anyOf": [
        {
          "$ref": "#/$defs/projections",
          "description": "Optional registry of custom or named projections."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Optional registry of custom or named projections."
    },
    "sources": {
      "anyOf": [
        {
          "$ref": "#/$defs/sources",
          "description": "Data source registry. Required."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Data source registry. Required."
    },
    "styles": {
      "anyOf": [
        {
          "$ref": "#/$defs/styles",
          "description": "Optional style registry referenced by layers and tilesets."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Optional style registry referenced by layers and tilesets."
    },
    "layers": {
      "anyOf": [
        {
          "$ref": "#/$defs/layers",
          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
    },
    "$defs": {
      "type": "object",
      "additionalProperties": {
        "description": "Valeur JSON libre utilisable dans la section racine $defs de la configuration"
      },
      "description": "Définitions de configuration réutilisables avant expansion"
    }
  },
  "$defs": {
    "httpPath": {
      "anyOf": [
        {
          "title": "HTTP path",
          "type": "string",
          "pattern": "^/",
          "description": "HTTP route path used by a service endpoint. Pattern `^/`: the path must start with `/`, for example `/wms`, `/api`, `/tiles`. Pattern `^/`: the value must start with `/`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
        }
      ],
      "description": "HTTP route path used by a service endpoint. Pattern `^/`: the path must start with `/`, for example `/wms`, `/api`, `/tiles`. Pattern `^/`: the value must start with `/`."
    },
    "describedEntry": {
      "anyOf": [
        {
          "title": "Common descriptive fields",
          "type": "object",
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            }
          },
          "description": "Reusable descriptive metadata shared by sources, layers, styles and tilesets."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Reusable descriptive metadata shared by sources, layers, styles and tilesets."
    },
    "serviceInfo": {
      "anyOf": [
        {
          "title": "Common service fields",
          "type": "object",
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata."
            }
          },
          "description": "Reusable service metadata for OGC-like services."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
        }
      ],
      "description": "Reusable service metadata for OGC-like services."
    },
    "bbox": {
      "anyOf": [
        {
          "title": "Bounding box",
          "type": "array",
          "prefixItems": [
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Minimum X coordinate of the bounding box."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Minimum X coordinate of the bounding box."
            },
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Minimum Y coordinate of the bounding box."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Minimum Y coordinate of the bounding box."
            },
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Maximum X coordinate of the bounding box. Must be greater than minimum X at runtime."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Maximum X coordinate of the bounding box. Must be greater than minimum X at runtime."
            },
            {
              "anyOf": [
                {
                  "type": "number",
                  "description": "Maximum Y coordinate of the bounding box. Must be greater than minimum Y at runtime."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Maximum Y coordinate of the bounding box. Must be greater than minimum Y at runtime."
            }
          ],
          "items": false,
          "minItems": 4,
          "maxItems": 4,
          "description": "Bounding box as `[minX, minY, maxX, maxY]` in the CRS of the related layer or request. Exactly 4 numbers are required. Array length: minimum 4, maximum 4."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "Bounding box as `[minX, minY, maxX, maxY]` in the CRS of the related layer or request. Exactly 4 numbers are required. Array length: minimum 4, maximum 4."
    },
    "server": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "port": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 65535,
                  "description": "HTTP server port. Minimum: 0. Maximum: 65535."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "HTTP server port. Minimum: 0. Maximum: 65535."
            },
            "logLevel": {
              "anyOf": [
                {
                  "type": "string",
                  "enum": [
                    "DEBUG",
                    "LOG",
                    "WARN",
                    "ERROR",
                    "NONE"
                  ],
                  "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`."
            }
          },
          "description": "Server runtime configuration."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "port": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "maximum": 65535,
                  "description": "HTTP server port. Minimum: 0. Maximum: 65535."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "HTTP server port. Minimum: 0. Maximum: 65535."
            },
            "logLevel": {
              "anyOf": [
                {
                  "type": "string",
                  "enum": [
                    "DEBUG",
                    "LOG",
                    "WARN",
                    "ERROR",
                    "NONE"
                  ],
                  "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Application log verbosity. Allowed values: `DEBUG`, `LOG`, `WARN`, `ERROR`, `NONE`."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "Server runtime configuration."
    },
    "services": {
      "anyOf": [
        {
          "title": "Enabled services",
          "type": "object",
          "minProperties": 1,
          "additionalProperties": false,
          "properties": {
            "wms": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmsService",
                  "description": "WMS service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "maxWidth": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image width in pixels. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum WMS map image width in pixels. Minimum: 1."
                    },
                    "maxHeight": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image height in pixels. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum WMS map image height in pixels. Minimum: 1."
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "WMS service configuration."
            },
            "api": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ogcFeaturesService",
                  "description": "OGC API Features service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "defaultLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                    },
                    "maxLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "OGC API Features service configuration."
            },
            "xyz": {
              "anyOf": [
                {
                  "$ref": "#/$defs/xyzService",
                  "description": "XYZ tile service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "maxScaleFactor": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "XYZ tile service configuration."
            },
            "wmts": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmtsService",
                  "description": "WMTS service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "WMTS service configuration."
            }
          },
          "description": "Enabled GeoComposer services. At least one of `wms`, `api`, `xyz`, or `wmts` must be present. Must contain at least 1 property/properties."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "wms": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmsService",
                  "description": "WMS service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "maxWidth": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image width in pixels. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum WMS map image width in pixels. Minimum: 1."
                    },
                    "maxHeight": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum WMS map image height in pixels. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum WMS map image height in pixels. Minimum: 1."
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "WMS service configuration."
            },
            "api": {
              "anyOf": [
                {
                  "$ref": "#/$defs/ogcFeaturesService",
                  "description": "OGC API Features service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "defaultLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                    },
                    "maxLimit": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                    },
                    "supportedCrs": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                    },
                    "layers": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          "uniqueItems": true,
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "OGC API Features service configuration."
            },
            "xyz": {
              "anyOf": [
                {
                  "$ref": "#/$defs/xyzService",
                  "description": "XYZ tile service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "maxScaleFactor": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "XYZ tile service configuration."
            },
            "wmts": {
              "anyOf": [
                {
                  "$ref": "#/$defs/wmtsService",
                  "description": "WMTS service configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "cache": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                    },
                    "tilesets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/tilesetRefs",
                          "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "WMTS service configuration."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "Enabled GeoComposer services. At least one of `wms`, `api`, `xyz`, or `wmts` must be present. Must contain at least 1 property/properties."
    },
    "wmsService": {
      "anyOf": [
        {
          "title": "WMS service",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/serviceInfo",
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`."
            }
          ],
          "required": [
            "title"
          ],
          "properties": {
            "maxWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image width in pixels. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum WMS map image width in pixels. Minimum: 1."
            },
            "maxHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image height in pixels. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum WMS map image height in pixels. Minimum: 1."
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
            }
          },
          "unevaluatedProperties": false,
          "description": "WMS service configuration. Requires `title`. Use `supportedCrs` to restrict advertised CRS values and `layers` to expose only selected layers."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata."
            },
            "maxWidth": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image width in pixels. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum WMS map image width in pixels. Minimum: 1."
            },
            "maxHeight": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum WMS map image height in pixels. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum WMS map image height in pixels. Minimum: 1."
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "WMS service configuration. Requires `title`. Use `supportedCrs` to restrict advertised CRS values and `layers` to expose only selected layers."
    },
    "ogcFeaturesService": {
      "anyOf": [
        {
          "title": "OGC API Features service",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/serviceInfo",
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`."
            }
          ],
          "required": [
            "title"
          ],
          "properties": {
            "defaultLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
            },
            "maxLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
            }
          },
          "unevaluatedProperties": false,
          "description": "OGC API Features service configuration. Requires `title`. Use `defaultLimit` and `maxLimit` to control pagination."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata."
            },
            "defaultLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Default number of features returned by OGC API Features when the client does not provide a limit. Minimum: 1."
            },
            "maxLimit": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum number of features allowed per OGC API Features response. Minimum: 1."
            },
            "supportedCrs": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of CRS codes supported or advertised by the service, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`. Values must be unique."
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                  },
                  "uniqueItems": true,
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Values must be unique."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "OGC API Features service configuration. Requires `title`. Use `defaultLimit` and `maxLimit` to control pagination."
    },
    "xyzService": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "maxScaleFactor": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
            },
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
            }
          },
          "description": "XYZ tile service configuration."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "maxScaleFactor": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Maximum tile oversampling or scale factor accepted by the XYZ service. Must be greater than 0."
            },
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "XYZ tile service configuration."
    },
    "wmtsService": {
      "anyOf": [
        {
          "title": "WMTS service",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/serviceInfo",
                  "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "path": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/httpPath",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "onlineResource": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Public service URL advertised in service capabilities metadata."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Public service URL advertised in service capabilities metadata."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common service fields: `path`, `title`, `abstract`, and `onlineResource`."
            }
          ],
          "required": [
            "title"
          ],
          "properties": {
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
            }
          },
          "unevaluatedProperties": false,
          "description": "WMTS service configuration. Requires `title`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "path": {
              "anyOf": [
                {
                  "$ref": "#/$defs/httpPath",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "onlineResource": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Public service URL advertised in service capabilities metadata."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Public service URL advertised in service capabilities metadata."
            },
            "cache": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem directory or cache identifier used to store generated tiles/responses."
            },
            "tilesets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/tilesetRefs",
                  "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of tileset names. Values must reference keys from the top-level `tilesets` registry."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "WMTS service configuration. Requires `title`."
    },
    "tilesetRefs": {
      "anyOf": [
        {
          "title": "Tileset name list",
          "type": "array",
          "minItems": 1,
          "items": {
            "anyOf": [
              {
                "type": "string",
                "description": "Tileset name. Must match a key from the top-level `tilesets` registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              },
              {
                "type": "string",
                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
              },
              {
                "type": "string",
                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
              }
            ],
            "description": "Tileset name. Must match a key from the top-level `tilesets` registry."
          },
          "uniqueItems": true,
          "description": "List of tileset names. Each value must match a key from the top-level `tilesets` registry. At least one unique value is required. Array length: minimum 1. Values must be unique."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "List of tileset names. Each value must match a key from the top-level `tilesets` registry. At least one unique value is required. Array length: minimum 1. Values must be unique."
    },
    "tilesets": {
      "anyOf": [
        {
          "title": "Tileset registry",
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/tileset",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients."
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients."
                  },
                  "tileMatrixSet": {
                    "anyOf": [
                      {
                        "enum": [
                          "WebMercatorQuad"
                        ],
                        "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
                  },
                  "formats": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "uniqueItems": true,
                        "items": {
                          "anyOf": [
                            {
                              "enum": [
                                "image/png",
                                "application/geo+json",
                                "application/vnd.mapbox-vector-tile"
                              ],
                              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            },
                            {
                              "type": "string",
                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                              "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                            },
                            {
                              "type": "string",
                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                              "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                            }
                          ],
                          "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                        },
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
                  },
                  "tileSize": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 1,
                        "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                      }
                    ],
                    "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
                  },
                  "minZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Minimum zoom level available for the tileset. Minimum: 0."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                      }
                    ],
                    "description": "Minimum zoom level available for the tileset. Minimum: 0."
                  },
                  "maxZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                      }
                    ],
                    "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
                  },
                  "cacheControl": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
                  },
                  "vector": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/vectorTileOptions",
                        "description": "Vector tile generation options."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "extent": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                              }
                            ],
                            "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                          },
                          "buffer": {
                            "anyOf": [
                              {
                                "type": "number",
                                "minimum": 0,
                                "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                              }
                            ],
                            "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                          },
                          "generalization": {
                            "anyOf": [
                              {
                                "type": "object",
                                "additionalProperties": false,
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                  }
                                },
                                "description": "Geometry simplification options applied during vector tile generation."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                  }
                                },
                                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                                "additionalProperties": false
                              }
                            ],
                            "description": "Geometry simplification options applied during vector tile generation."
                          },
                          "geojsonPrecision": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 0,
                                "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                              }
                            ],
                            "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                          },
                          "maxFeatures": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Maximum number of features emitted per tile. Minimum: 1."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                              }
                            ],
                            "description": "Maximum number of features emitted per tile. Minimum: 1."
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                            "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                          }
                        },
                        "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                      }
                    ],
                    "description": "Vector tile generation options."
                  },
                  "layers": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "anyOf": [
                            {
                              "anyOf": [
                                {
                                  "anyOf": [
                                    {
                                      "type": "string",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                },
                                {
                                  "anyOf": [
                                    {
                                      "$ref": "#/$defs/tilesetLayer",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "object",
                                      "required": [
                                        "$ref"
                                      ],
                                      "properties": {
                                        "layer": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                              "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                              "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                            }
                                          ],
                                          "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                        },
                                        "style": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Style name. Must reference a key from the top-level `styles` registry."
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                              "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                              "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                            }
                                          ],
                                          "description": "Style name. Must reference a key from the top-level `styles` registry."
                                        },
                                        "$ref": {
                                          "type": "string",
                                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                        }
                                      },
                                      "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                }
                              ],
                              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          ],
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                        },
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                  }
                },
                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "Named tileset registry."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/tileset",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients."
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients."
                  },
                  "tileMatrixSet": {
                    "anyOf": [
                      {
                        "enum": [
                          "WebMercatorQuad"
                        ],
                        "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
                  },
                  "formats": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "uniqueItems": true,
                        "items": {
                          "anyOf": [
                            {
                              "enum": [
                                "image/png",
                                "application/geo+json",
                                "application/vnd.mapbox-vector-tile"
                              ],
                              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            },
                            {
                              "type": "string",
                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                              "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                            },
                            {
                              "type": "string",
                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                              "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                            }
                          ],
                          "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                        },
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
                  },
                  "tileSize": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 1,
                        "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                      }
                    ],
                    "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
                  },
                  "minZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Minimum zoom level available for the tileset. Minimum: 0."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                      }
                    ],
                    "description": "Minimum zoom level available for the tileset. Minimum: 0."
                  },
                  "maxZoom": {
                    "anyOf": [
                      {
                        "type": "integer",
                        "minimum": 0,
                        "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                      }
                    ],
                    "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
                  },
                  "cacheControl": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
                  },
                  "vector": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/vectorTileOptions",
                        "description": "Vector tile generation options."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "extent": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                              }
                            ],
                            "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                          },
                          "buffer": {
                            "anyOf": [
                              {
                                "type": "number",
                                "minimum": 0,
                                "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                              }
                            ],
                            "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                          },
                          "generalization": {
                            "anyOf": [
                              {
                                "type": "object",
                                "additionalProperties": false,
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                  }
                                },
                                "description": "Geometry simplification options applied during vector tile generation."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "tolerance": {
                                    "anyOf": [
                                      {
                                        "type": "number",
                                        "minimum": 0,
                                        "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                      }
                                    ],
                                    "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                  }
                                },
                                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                                "additionalProperties": false
                              }
                            ],
                            "description": "Geometry simplification options applied during vector tile generation."
                          },
                          "geojsonPrecision": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 0,
                                "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                              }
                            ],
                            "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                          },
                          "maxFeatures": {
                            "anyOf": [
                              {
                                "type": "integer",
                                "minimum": 1,
                                "description": "Maximum number of features emitted per tile. Minimum: 1."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                              }
                            ],
                            "description": "Maximum number of features emitted per tile. Minimum: 1."
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                            "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                          }
                        },
                        "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                      }
                    ],
                    "description": "Vector tile generation options."
                  },
                  "layers": {
                    "anyOf": [
                      {
                        "type": "array",
                        "minItems": 1,
                        "items": {
                          "anyOf": [
                            {
                              "anyOf": [
                                {
                                  "anyOf": [
                                    {
                                      "type": "string",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                },
                                {
                                  "anyOf": [
                                    {
                                      "$ref": "#/$defs/tilesetLayer",
                                      "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "object",
                                      "required": [
                                        "$ref"
                                      ],
                                      "properties": {
                                        "layer": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                              "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                              "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                            }
                                          ],
                                          "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                        },
                                        "style": {
                                          "anyOf": [
                                            {
                                              "type": "string",
                                              "description": "Style name. Must reference a key from the top-level `styles` registry."
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                              "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                            },
                                            {
                                              "type": "string",
                                              "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                              "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                            }
                                          ],
                                          "description": "Style name. Must reference a key from the top-level `styles` registry."
                                        },
                                        "$ref": {
                                          "type": "string",
                                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                        }
                                      },
                                      "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                                    }
                                  ],
                                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                                }
                              ],
                              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          ],
                          "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                        },
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                  }
                },
                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "Named tileset registry."
    },
    "tileset": {
      "anyOf": [
        {
          "title": "Tileset",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "formats",
            "layers"
          ],
          "properties": {
            "tileMatrixSet": {
              "anyOf": [
                {
                  "enum": [
                    "WebMercatorQuad"
                  ],
                  "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
            },
            "formats": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "uniqueItems": true,
                  "items": {
                    "anyOf": [
                      {
                        "enum": [
                          "image/png",
                          "application/geo+json",
                          "application/vnd.mapbox-vector-tile"
                        ],
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                  },
                  "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
            },
            "tileSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
            },
            "minZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Minimum zoom level available for the tileset. Minimum: 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Minimum zoom level available for the tileset. Minimum: 0."
            },
            "maxZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
            },
            "cacheControl": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
            },
            "vector": {
              "anyOf": [
                {
                  "$ref": "#/$defs/vectorTileOptions",
                  "description": "Vector tile generation options."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "extent": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                    },
                    "buffer": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                    },
                    "generalization": {
                      "anyOf": [
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                            }
                          },
                          "description": "Geometry simplification options applied during vector tile generation."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                            },
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          },
                          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                          "additionalProperties": false
                        }
                      ],
                      "description": "Geometry simplification options applied during vector tile generation."
                    },
                    "geojsonPrecision": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                    },
                    "maxFeatures": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features emitted per tile. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum number of features emitted per tile. Minimum: 1."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Vector tile generation options."
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "anyOf": [
                      {
                        "anyOf": [
                          {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          {
                            "anyOf": [
                              {
                                "$ref": "#/$defs/tilesetLayer",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "layer": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                      }
                                    ],
                                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                  },
                                  "style": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Style name. Must reference a key from the top-level `styles` registry."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                      }
                                    ],
                                    "description": "Style name. Must reference a key from the top-level `styles` registry."
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                  }
                                },
                                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          }
                        ],
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                  },
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
            }
          },
          "unevaluatedProperties": false,
          "description": "Tileset definition. Requires `formats` and `layers`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "tileMatrixSet": {
              "anyOf": [
                {
                  "enum": [
                    "WebMercatorQuad"
                  ],
                  "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Tile matrix set identifier. Allowed values: `WebMercatorQuad`."
            },
            "formats": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "uniqueItems": true,
                  "items": {
                    "anyOf": [
                      {
                        "enum": [
                          "image/png",
                          "application/geo+json",
                          "application/vnd.mapbox-vector-tile"
                        ],
                        "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`."
                  },
                  "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of output formats supported by this tileset. Allowed values: `image/png`, `application/geo+json`, `application/vnd.mapbox-vector-tile`. Array length: minimum 1. Values must be unique."
            },
            "tileSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Tile width and height in pixels. Minimum: 1. Common value: 256."
            },
            "minZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Minimum zoom level available for the tileset. Minimum: 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Minimum zoom level available for the tileset. Minimum: 0."
            },
            "maxZoom": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum zoom level available for the tileset. Minimum: 0. Should be greater than or equal to `minZoom`."
            },
            "cacheControl": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "HTTP `Cache-Control` header value for tile responses, for example `public, max-age=86400`."
            },
            "vector": {
              "anyOf": [
                {
                  "$ref": "#/$defs/vectorTileOptions",
                  "description": "Vector tile generation options."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "extent": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                    },
                    "buffer": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                    },
                    "generalization": {
                      "anyOf": [
                        {
                          "type": "object",
                          "additionalProperties": false,
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                            }
                          },
                          "description": "Geometry simplification options applied during vector tile generation."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "tolerance": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "minimum": 0,
                                  "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                }
                              ],
                              "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                            },
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          },
                          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                          "additionalProperties": false
                        }
                      ],
                      "description": "Geometry simplification options applied during vector tile generation."
                    },
                    "geojsonPrecision": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                    },
                    "maxFeatures": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum number of features emitted per tile. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum number of features emitted per tile. Minimum: 1."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Vector tile generation options."
            },
            "layers": {
              "anyOf": [
                {
                  "type": "array",
                  "minItems": 1,
                  "items": {
                    "anyOf": [
                      {
                        "anyOf": [
                          {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          },
                          {
                            "anyOf": [
                              {
                                "$ref": "#/$defs/tilesetLayer",
                                "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "object",
                                "required": [
                                  "$ref"
                                ],
                                "properties": {
                                  "layer": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                      }
                                    ],
                                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                                  },
                                  "style": {
                                    "anyOf": [
                                      {
                                        "type": "string",
                                        "description": "Style name. Must reference a key from the top-level `styles` registry."
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                      },
                                      {
                                        "type": "string",
                                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                      }
                                    ],
                                    "description": "Style name. Must reference a key from the top-level `styles` registry."
                                  },
                                  "$ref": {
                                    "type": "string",
                                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                  }
                                },
                                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                              }
                            ],
                            "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                          }
                        ],
                        "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here."
                  },
                  "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Unique list of layer names to expose or include. Values must reference keys from the top-level `layers` registry, unless an object form is allowed here. Array length: minimum 1."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "Tileset definition. Requires `formats` and `layers`."
    },
    "vectorTileOptions": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "extent": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
            },
            "buffer": {
              "anyOf": [
                {
                  "type": "number",
                  "minimum": 0,
                  "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
            },
            "generalization": {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                    }
                  },
                  "description": "Geometry simplification options applied during vector tile generation."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                  "additionalProperties": false
                }
              ],
              "description": "Geometry simplification options applied during vector tile generation."
            },
            "geojsonPrecision": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
            },
            "maxFeatures": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features emitted per tile. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum number of features emitted per tile. Minimum: 1."
            }
          },
          "description": "Options used when generating vector tiles or GeoJSON-like tile output."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "extent": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Vector tile coordinate extent. Minimum: 1. Common value: 4096."
            },
            "buffer": {
              "anyOf": [
                {
                  "type": "number",
                  "minimum": 0,
                  "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Tile buffer size. Minimum: 0. Used to include nearby features outside the tile boundary."
            },
            "generalization": {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                    }
                  },
                  "description": "Geometry simplification options applied during vector tile generation."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "tolerance": {
                      "anyOf": [
                        {
                          "type": "number",
                          "minimum": 0,
                          "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Simplification tolerance. Minimum: 0. Higher values simplify geometries more aggressively."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                  "additionalProperties": false
                }
              ],
              "description": "Geometry simplification options applied during vector tile generation."
            },
            "geojsonPrecision": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 0,
                  "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Number of decimal digits kept in GeoJSON coordinate output. Minimum: 0."
            },
            "maxFeatures": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Maximum number of features emitted per tile. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Maximum number of features emitted per tile. Minimum: 1."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "Options used when generating vector tiles or GeoJSON-like tile output."
    },
    "tilesetLayer": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "layer"
          ],
          "properties": {
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry."
            }
          },
          "description": "Layer entry inside a tileset. Allows selecting a layer and optionally overriding its style."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "Layer entry inside a tileset. Allows selecting a layer and optionally overriding its style."
    },
    "projections": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/projection",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "name": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
                  },
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients."
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                  }
                },
                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "Projection registry. Each property name is a CRS code/projection identifier."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/projection",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "name": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
                  },
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients."
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                  }
                },
                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "Projection registry. Each property name is a CRS code/projection identifier."
    },
    "projection": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "title"
          ],
          "properties": {
            "name": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            }
          },
          "description": "Projection metadata. Requires a human-readable `title`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "name": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Machine or external name. For projections, this may be the projection name known by the projection library."
            },
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "Projection metadata. Requires a human-readable `title`."
    },
    "sources": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/source",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "Source registry. At least one source is required. Must contain at least 1 property/properties."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/source",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "Source registry. At least one source is required. Must contain at least 1 property/properties."
    },
    "source": {
      "anyOf": [
        {
          "oneOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/geojsonSource",
                  "description": "First allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "geojson",
                          "description": "Source discriminator. Must be `geojson`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Source discriminator. Must be `geojson`."
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "encoding": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                    },
                    "highWaterMark": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Stream buffer size. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Stream buffer size. Minimum: 1."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "First allowed object shape for this union."
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/gmlSource",
                  "description": "Second allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "gml",
                          "description": "Source discriminator. Must be `gml`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Source discriminator. Must be `gml`."
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "encoding": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                    },
                    "highWaterMark": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Stream buffer size. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Stream buffer size. Minimum: 1."
                    },
                    "featureElementNames": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                          },
                          "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                    },
                    "geometryPropertyNames": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                          },
                          "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                    },
                    "axisOrder": {
                      "anyOf": [
                        {
                          "enum": [
                            "auto",
                            "xy",
                            "yx"
                          ],
                          "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Second allowed object shape for this union."
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/shpSource",
                  "description": "Shapefile source variant."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "shp",
                          "description": "Source discriminator. Must be `shp`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Source discriminator. Must be `shp`."
                    },
                    "shpPath": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Path to the `.shp` file of the Shapefile source."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Path to the `.shp` file of the Shapefile source."
                    },
                    "dbfPath": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Path to the `.dbf` file of the Shapefile source."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Path to the `.dbf` file of the Shapefile source."
                    },
                    "dbfEncoding": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`."
                    },
                    "highWaterMark": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Stream buffer size. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Stream buffer size. Minimum: 1."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Shapefile source variant."
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/gpkgSource",
                  "description": "GeoPackage source variant."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "gpkg",
                          "description": "Source discriminator. Must be `gpkg`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Source discriminator. Must be `gpkg`."
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "datasets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/gpkgDatasets",
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          },
                          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                        }
                      ],
                      "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "GeoPackage source variant."
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/postgisSource",
                  "description": "PostGIS source variant."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "postgis",
                          "description": "Source discriminator. Must be `postgis`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Source discriminator. Must be `postgis`."
                    },
                    "connection": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/postgisConnection",
                          "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                    },
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "datasets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sqlDatasets",
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          },
                          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                        }
                      ],
                      "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                    },
                    "batchSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of features/rows fetched per batch. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Number of features/rows fetched per batch. Minimum: 1."
                    },
                    "extentStrategy": {
                      "anyOf": [
                        {
                          "enum": [
                            "estimated",
                            "exact",
                            "none"
                          ],
                          "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "PostGIS source variant."
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleSource",
                  "description": "Oracle source variant."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "oracle",
                          "description": "Source discriminator. Must be `oracle`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Source discriminator. Must be `oracle`."
                    },
                    "connection": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/oracleConnection",
                          "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                    },
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "datasets": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/sqlDatasets",
                          "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          },
                          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                        }
                      ],
                      "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                    },
                    "batchSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of features/rows fetched per batch. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Number of features/rows fetched per batch. Minimum: 1."
                    },
                    "extentStrategy": {
                      "anyOf": [
                        {
                          "enum": [
                            "metadata",
                            "exact",
                            "none"
                          ],
                          "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Oracle source variant."
            }
          ],
          "description": "Data source definition. Choose exactly one supported source type: GeoJSON, GML, Shapefile, GeoPackage, PostGIS or Oracle."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "Data source definition. Choose exactly one supported source type: GeoJSON, GML, Shapefile, GeoPackage, PostGIS or Oracle."
    },
    "geojsonSource": {
      "anyOf": [
        {
          "title": "GeoJSON source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type",
            "path"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "geojson",
                  "description": "Source discriminator. Must be `geojson`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `geojson`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Stream buffer size. Minimum: 1."
            }
          },
          "unevaluatedProperties": false,
          "description": "GeoJSON file source. Requires `type: \"geojson\"` and `path`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "geojson",
                  "description": "Source discriminator. Must be `geojson`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `geojson`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Stream buffer size. Minimum: 1."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "GeoJSON file source. Requires `type: \"geojson\"` and `path`."
    },
    "gmlSource": {
      "anyOf": [
        {
          "title": "GML source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type",
            "path"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "gml",
                  "description": "Source discriminator. Must be `gml`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `gml`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Stream buffer size. Minimum: 1."
            },
            "featureElementNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                  },
                  "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
            },
            "geometryPropertyNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                  },
                  "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
            },
            "axisOrder": {
              "anyOf": [
                {
                  "enum": [
                    "auto",
                    "xy",
                    "yx"
                  ],
                  "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`."
            }
          },
          "unevaluatedProperties": false,
          "description": "GML file source. Requires `type: \"gml\"` and `path`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "gml",
                  "description": "Source discriminator. Must be `gml`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `gml`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "encoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Text encoding used to read the file, for example `utf8`, `latin1` or `windows-1252`."
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Stream buffer size. Minimum: 1."
            },
            "featureElementNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                  },
                  "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "GML feature element names to read. Useful when auto-detection is ambiguous."
            },
            "geometryPropertyNames": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                  },
                  "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "GML geometry property names to read. Useful when auto-detection is ambiguous."
            },
            "axisOrder": {
              "anyOf": [
                {
                  "enum": [
                    "auto",
                    "xy",
                    "yx"
                  ],
                  "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate axis order for GML coordinates. Allowed values: `auto`, `xy`, `yx`."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "GML file source. Requires `type: \"gml\"` and `path`."
    },
    "shpSource": {
      "anyOf": [
        {
          "title": "Shapefile source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type",
            "shpPath",
            "dbfPath"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "shp",
                  "description": "Source discriminator. Must be `shp`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `shp`."
            },
            "shpPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.shp` file of the Shapefile source."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Path to the `.shp` file of the Shapefile source."
            },
            "dbfPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.dbf` file of the Shapefile source."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Path to the `.dbf` file of the Shapefile source."
            },
            "dbfEncoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`."
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Stream buffer size. Minimum: 1."
            }
          },
          "unevaluatedProperties": false,
          "description": "Shapefile source. Requires `type: \"shp\"`, `shpPath` and `dbfPath`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "shp",
                  "description": "Source discriminator. Must be `shp`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `shp`."
            },
            "shpPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.shp` file of the Shapefile source."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Path to the `.shp` file of the Shapefile source."
            },
            "dbfPath": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Path to the `.dbf` file of the Shapefile source."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Path to the `.dbf` file of the Shapefile source."
            },
            "dbfEncoding": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Character encoding used to read the DBF file, for example `utf8`, `latin1` or `windows-1252`."
            },
            "highWaterMark": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Stream buffer size. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Stream buffer size. Minimum: 1."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "Shapefile source. Requires `type: \"shp\"`, `shpPath` and `dbfPath`."
    },
    "gpkgSource": {
      "anyOf": [
        {
          "title": "GeoPackage source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type",
            "path",
            "datasets"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "gpkg",
                  "description": "Source discriminator. Must be `gpkg`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `gpkg`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/gpkgDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
            }
          },
          "unevaluatedProperties": false,
          "description": "GeoPackage source. Requires `type: \"gpkg\"`, `path` and `datasets`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "gpkg",
                  "description": "Source discriminator. Must be `gpkg`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `gpkg`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/gpkgDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "GeoPackage source. Requires `type: \"gpkg\"`, `path` and `datasets`."
    },
    "gpkgDatasets": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/gpkgDataset",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "GeoPackage dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/gpkgDataset",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "GeoPackage dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties."
    },
    "gpkgDataset": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s)."
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                    }
                  },
                  "description": "Object form for explicit configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration."
            }
          ],
          "description": "GeoPackage dataset mapping. Use a string for a simple table name, or an object for explicit table, geometry, primary key and property selection."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "GeoPackage dataset mapping. Use a string for a simple table name, or an object for explicit table, geometry, primary key and property selection."
    },
    "sqlDatasets": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/sqlDataset",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "SQL dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/sqlDataset",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "SQL dataset registry. Each property name is the dataset identifier exposed to layers. At least one dataset is required. Must contain at least 1 property/properties."
    },
    "sqlDataset": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Short form using a non-empty name/string value. Minimum length: 1 character(s)."
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "srid": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Spatial reference identifier of the dataset geometry. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Spatial reference identifier of the dataset geometry. Minimum: 1."
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                    }
                  },
                  "description": "Object form for explicit configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "schema": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "tableName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database or GeoPackage table name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "geometryColumn": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the geometry column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "primaryKey": {
                      "anyOf": [
                        {
                          "type": "string",
                          "minLength": 1,
                          "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Name of the primary key column. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                    },
                    "srid": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Spatial reference identifier of the dataset geometry. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Spatial reference identifier of the dataset geometry. Minimum: 1."
                    },
                    "properties": {
                      "anyOf": [
                        {
                          "type": "array",
                          "items": {
                            "anyOf": [
                              {
                                "type": "string",
                                "minLength": 1,
                                "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "List of attribute/property names to expose. Values must be unique when constrained. Minimum length: 1 character(s)."
                          },
                          "uniqueItems": true,
                          "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        }
                      ],
                      "description": "List of attribute/property names to expose. Values must be unique when constrained. Values must be unique."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration."
            }
          ],
          "description": "SQL dataset mapping. Use a string for a simple table name, or an object for explicit schema, table, geometry, primary key, SRID and property selection."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "SQL dataset mapping. Use a string for a simple table name, or an object for explicit schema, table, geometry, primary key, SRID and property selection."
    },
    "postgisSource": {
      "anyOf": [
        {
          "title": "PostGIS source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type",
            "connection",
            "datasets"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "postgis",
                  "description": "Source discriminator. Must be `postgis`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `postgis`."
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/postgisConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1."
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "estimated",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`."
            }
          },
          "unevaluatedProperties": false,
          "description": "PostGIS source. Requires `type: \"postgis\"`, `connection` and `datasets`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "postgis",
                  "description": "Source discriminator. Must be `postgis`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `postgis`."
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/postgisConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1."
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "estimated",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `estimated`, `exact`, `none`."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "PostGIS source. Requires `type: \"postgis\"`, `connection` and `datasets`."
    },
    "postgisConnection": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Short form using a string value."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Short form using a string value."
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                    },
                    "host": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database server host name or IP address."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database server host name or IP address."
                    },
                    "port": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 65535,
                          "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535."
                    },
                    "database": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database name."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database name."
                    },
                    "user": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database user name."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database user name."
                    },
                    "password": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database password. Prefer environment placeholders instead of committing secrets."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database password. Prefer environment placeholders instead of committing secrets."
                    },
                    "ssl": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether SSL/TLS is enabled for the database connection."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement booléenne : $b{VAR} ou $b{VAR|false}"
                        }
                      ],
                      "description": "Whether SSL/TLS is enabled for the database connection."
                    },
                    "max": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum PostgreSQL pool size. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum PostgreSQL pool size. Minimum: 1."
                    },
                    "connectionTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Connection timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Connection timeout in milliseconds. Minimum: 0."
                    },
                    "idleTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Idle connection timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Idle connection timeout in milliseconds. Minimum: 0."
                    },
                    "statementTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0."
                    },
                    "queryTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL query timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "PostgreSQL query timeout in milliseconds. Minimum: 0."
                    },
                    "applicationName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Application name sent to PostgreSQL for connection identification."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Application name sent to PostgreSQL for connection identification."
                    }
                  },
                  "description": "Object form for explicit configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                    },
                    "host": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database server host name or IP address."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database server host name or IP address."
                    },
                    "port": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "maximum": 65535,
                          "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "PostgreSQL server port. Minimum: 1. Maximum: 65535."
                    },
                    "database": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database name."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database name."
                    },
                    "user": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database user name."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database user name."
                    },
                    "password": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Database password. Prefer environment placeholders instead of committing secrets."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database password. Prefer environment placeholders instead of committing secrets."
                    },
                    "ssl": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether SSL/TLS is enabled for the database connection."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement booléenne : $b{VAR} ou $b{VAR|false}"
                        }
                      ],
                      "description": "Whether SSL/TLS is enabled for the database connection."
                    },
                    "max": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum PostgreSQL pool size. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum PostgreSQL pool size. Minimum: 1."
                    },
                    "connectionTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Connection timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Connection timeout in milliseconds. Minimum: 0."
                    },
                    "idleTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Idle connection timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Idle connection timeout in milliseconds. Minimum: 0."
                    },
                    "statementTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "PostgreSQL statement timeout in milliseconds. Minimum: 0."
                    },
                    "queryTimeoutMillis": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "PostgreSQL query timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "PostgreSQL query timeout in milliseconds. Minimum: 0."
                    },
                    "applicationName": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Application name sent to PostgreSQL for connection identification."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Application name sent to PostgreSQL for connection identification."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration."
            }
          ],
          "description": "PostgreSQL/PostGIS connection configuration. Use either a connection string or an object with individual connection pool options."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "PostgreSQL/PostGIS connection configuration. Use either a connection string or an object with individual connection pool options."
    },
    "oracleSource": {
      "anyOf": [
        {
          "title": "Oracle source",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type",
            "connection",
            "datasets"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "oracle",
                  "description": "Source discriminator. Must be `oracle`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `oracle`."
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1."
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "metadata",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`."
            }
          },
          "unevaluatedProperties": false,
          "description": "Oracle Spatial source. Requires `type: \"oracle\"`, `connection` and `datasets`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "oracle",
                  "description": "Source discriminator. Must be `oracle`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source discriminator. Must be `oracle`."
            },
            "connection": {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleConnection",
                  "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Database connection configuration. Can be a connection string or an object, depending on the source type."
            },
            "schema": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Database schema name. Minimum length: 1 where constrained. Minimum length: 1 character(s)."
            },
            "datasets": {
              "anyOf": [
                {
                  "$ref": "#/$defs/sqlDatasets",
                  "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Dataset registry for this source. Each key is a dataset identifier referenced by layers."
            },
            "batchSize": {
              "anyOf": [
                {
                  "type": "integer",
                  "minimum": 1,
                  "description": "Number of features/rows fetched per batch. Minimum: 1."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                }
              ],
              "description": "Number of features/rows fetched per batch. Minimum: 1."
            },
            "extentStrategy": {
              "anyOf": [
                {
                  "enum": [
                    "metadata",
                    "exact",
                    "none"
                  ],
                  "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Strategy used to compute source extents. PostGIS allowed values: `estimated`, `exact`, `none`. Oracle allowed values: `metadata`, `exact`, `none`. Allowed values: `metadata`, `exact`, `none`."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "Oracle Spatial source. Requires `type: \"oracle\"`, `connection` and `datasets`."
    },
    "oracleConnectionString": {
      "anyOf": [
        {
          "title": "Oracle connection URI",
          "type": "string",
          "pattern": "^oracle://[^:@/]+:[^@/]+@[^/]+:[0-9]+/.+$",
          "description": "Oracle connection URI. Expected format: `oracle://user:password@host:port/service`. Pattern: `oracle://user:password@host:port/service`, with a user, password, host, numeric port and service name."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "string",
          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
        },
        {
          "type": "string",
          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
        }
      ],
      "description": "Oracle connection URI. Expected format: `oracle://user:password@host:port/service`. Pattern: `oracle://user:password@host:port/service`, with a user, password, host, numeric port and service name."
    },
    "oracleConnection": {
      "anyOf": [
        {
          "anyOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/oracleConnectionString",
                  "description": "Short form: Oracle connection URI string matching `oracle://user:password@host:port/service`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Short form: Oracle connection URI string matching `oracle://user:password@host:port/service`."
            },
            {
              "anyOf": [
                {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "connectionString"
                  ],
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/oracleConnectionString",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                    },
                    "externalAuth": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether Oracle external authentication is enabled."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement booléenne : $b{VAR} ou $b{VAR|false}"
                        }
                      ],
                      "description": "Whether Oracle external authentication is enabled."
                    },
                    "homogeneous": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether the Oracle pool uses homogeneous authentication."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement booléenne : $b{VAR} ou $b{VAR|false}"
                        }
                      ],
                      "description": "Whether the Oracle pool uses homogeneous authentication."
                    },
                    "poolMin": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Minimum Oracle pool size. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Minimum Oracle pool size. Minimum: 0."
                    },
                    "poolMax": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum Oracle pool size. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum Oracle pool size. Minimum: 1."
                    },
                    "poolIncrement": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of Oracle connections added when the pool grows. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Number of Oracle connections added when the pool grows. Minimum: 1."
                    },
                    "poolTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle idle pool timeout in seconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle idle pool timeout in seconds. Minimum: 0."
                    },
                    "queueTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle queue timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle queue timeout in milliseconds. Minimum: 0."
                    },
                    "stmtCacheSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle statement cache size. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle statement cache size. Minimum: 0."
                    },
                    "callTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle call timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle call timeout in milliseconds. Minimum: 0."
                    },
                    "walletLocation": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle wallet directory."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path to the Oracle wallet directory."
                    },
                    "walletPassword": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets."
                    },
                    "configDir": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle client/network configuration directory."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path to the Oracle client/network configuration directory."
                    }
                  },
                  "description": "Object form for explicit configuration."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "connectionString": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/oracleConnectionString",
                          "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Database connection string. For Oracle, must match `oracle://user:password@host:port/service`."
                    },
                    "externalAuth": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether Oracle external authentication is enabled."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement booléenne : $b{VAR} ou $b{VAR|false}"
                        }
                      ],
                      "description": "Whether Oracle external authentication is enabled."
                    },
                    "homogeneous": {
                      "anyOf": [
                        {
                          "type": "boolean",
                          "description": "Whether the Oracle pool uses homogeneous authentication."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$b\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement booléenne : $b{VAR} ou $b{VAR|false}"
                        }
                      ],
                      "description": "Whether the Oracle pool uses homogeneous authentication."
                    },
                    "poolMin": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Minimum Oracle pool size. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Minimum Oracle pool size. Minimum: 0."
                    },
                    "poolMax": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Maximum Oracle pool size. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Maximum Oracle pool size. Minimum: 1."
                    },
                    "poolIncrement": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 1,
                          "description": "Number of Oracle connections added when the pool grows. Minimum: 1."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Number of Oracle connections added when the pool grows. Minimum: 1."
                    },
                    "poolTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle idle pool timeout in seconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle idle pool timeout in seconds. Minimum: 0."
                    },
                    "queueTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle queue timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle queue timeout in milliseconds. Minimum: 0."
                    },
                    "stmtCacheSize": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle statement cache size. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle statement cache size. Minimum: 0."
                    },
                    "callTimeout": {
                      "anyOf": [
                        {
                          "type": "integer",
                          "minimum": 0,
                          "description": "Oracle call timeout in milliseconds. Minimum: 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$i\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement entière : $i{VAR} ou $i{VAR|3000}"
                        }
                      ],
                      "description": "Oracle call timeout in milliseconds. Minimum: 0."
                    },
                    "walletLocation": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle wallet directory."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path to the Oracle wallet directory."
                    },
                    "walletPassword": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Oracle wallet password. Prefer environment placeholders instead of committing secrets."
                    },
                    "configDir": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path to the Oracle client/network configuration directory."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path to the Oracle client/network configuration directory."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
                  "additionalProperties": false
                }
              ],
              "description": "Object form for explicit configuration."
            }
          ],
          "description": "Oracle connection configuration. Use either an Oracle connection URI string or an object containing `connectionString` and optional pool/wallet settings."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "Oracle connection configuration. Use either an Oracle connection URI string or an object containing `connectionString` and optional pool/wallet settings."
    },
    "styles": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/style",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "Style registry. Each property name is a style identifier."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/style",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "Style registry. Each property name is a style identifier."
    },
    "style": {
      "anyOf": [
        {
          "oneOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/builtinStyle",
                  "description": "First allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "builtin",
                          "description": "Style discriminator. Must be `builtin`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Style discriminator. Must be `builtin`."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "First allowed object shape for this union."
            },
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/dynamicStyle",
                  "description": "Second allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "type": {
                      "anyOf": [
                        {
                          "const": "dynamic",
                          "description": "Style discriminator. Must be `dynamic`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Style discriminator. Must be `dynamic`."
                    },
                    "path": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                    },
                    "options": {
                      "anyOf": [
                        {
                          "$ref": "#/$defs/dynamicStyleOptions",
                          "description": "Options passed to this style or renderer."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "object",
                          "required": [
                            "$ref"
                          ],
                          "properties": {
                            "units": {
                              "anyOf": [
                                {
                                  "enum": [
                                    "m",
                                    "dd"
                                  ],
                                  "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                },
                                {
                                  "type": "string",
                                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                }
                              ],
                              "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                            },
                            "dotsPerInch": {
                              "anyOf": [
                                {
                                  "type": "number",
                                  "exclusiveMinimum": 0,
                                  "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                                },
                                {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                },
                                {
                                  "type": "string",
                                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                                }
                              ],
                              "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                            },
                            "$ref": {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            }
                          },
                          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                        }
                      ],
                      "description": "Options passed to this style or renderer."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Second allowed object shape for this union."
            }
          ],
          "description": "Style definition. Choose either a built-in style or a dynamic style file."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        }
      ],
      "description": "Style definition. Choose either a built-in style or a dynamic style file."
    },
    "builtinStyle": {
      "anyOf": [
        {
          "title": "Builtin style",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "builtin",
                  "description": "Style discriminator. Must be `builtin`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style discriminator. Must be `builtin`."
            }
          },
          "unevaluatedProperties": false,
          "description": "Built-in renderer style. Requires `type: \"builtin\"`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "builtin",
                  "description": "Style discriminator. Must be `builtin`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style discriminator. Must be `builtin`."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "Built-in renderer style. Requires `type: \"builtin\"`."
    },
    "dynamicStyle": {
      "anyOf": [
        {
          "title": "Dynamic style file",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "required": [
            "type",
            "path"
          ],
          "properties": {
            "type": {
              "anyOf": [
                {
                  "const": "dynamic",
                  "description": "Style discriminator. Must be `dynamic`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style discriminator. Must be `dynamic`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "options": {
              "anyOf": [
                {
                  "$ref": "#/$defs/dynamicStyleOptions",
                  "description": "Options passed to this style or renderer."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "units": {
                      "anyOf": [
                        {
                          "enum": [
                            "m",
                            "dd"
                          ],
                          "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                    },
                    "dotsPerInch": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Options passed to this style or renderer."
            }
          },
          "unevaluatedProperties": false,
          "description": "Dynamic style loaded from a file. Requires `type: \"dynamic\"` and `path`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "type": {
              "anyOf": [
                {
                  "const": "dynamic",
                  "description": "Style discriminator. Must be `dynamic`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style discriminator. Must be `dynamic`."
            },
            "path": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Filesystem path or HTTP path depending on context. For service paths, the value must start with `/`."
            },
            "options": {
              "anyOf": [
                {
                  "$ref": "#/$defs/dynamicStyleOptions",
                  "description": "Options passed to this style or renderer."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "units": {
                      "anyOf": [
                        {
                          "enum": [
                            "m",
                            "dd"
                          ],
                          "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                    },
                    "dotsPerInch": {
                      "anyOf": [
                        {
                          "type": "number",
                          "exclusiveMinimum": 0,
                          "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                        }
                      ],
                      "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Options passed to this style or renderer."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "Dynamic style loaded from a file. Requires `type: \"dynamic\"` and `path`."
    },
    "dynamicStyleOptions": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "units": {
              "anyOf": [
                {
                  "enum": [
                    "m",
                    "dd"
                  ],
                  "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
            },
            "dotsPerInch": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
            }
          },
          "description": "Rendering options passed to a dynamic style."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "units": {
              "anyOf": [
                {
                  "enum": [
                    "m",
                    "dd"
                  ],
                  "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate units used by the dynamic style. Allowed values: `m` for meters, `dd` for decimal degrees. Allowed values: `m`, `dd`."
            },
            "dotsPerInch": {
              "anyOf": [
                {
                  "type": "number",
                  "exclusiveMinimum": 0,
                  "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$f\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement numérique : $f{VAR} ou $f{VAR|1.5}"
                }
              ],
              "description": "Rendering DPI used by the dynamic style. Must be greater than 0."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "Rendering options passed to a dynamic style."
    },
    "layers": {
      "anyOf": [
        {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/layer",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients."
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients."
                  },
                  "source": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Source name. Must reference a key from the top-level `sources` registry."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Source name. Must reference a key from the top-level `sources` registry."
                  },
                  "layer": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                  },
                  "dataset": {
                    "anyOf": [
                      {
                        "type": "string",
                        "minLength": 1,
                        "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
                  },
                  "crs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                  },
                  "extent": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/bbox",
                        "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
                  },
                  "style": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Style name. Must reference a key from the top-level `styles` registry."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Style name. Must reference a key from the top-level `styles` registry."
                  },
                  "pointProperties": {
                    "anyOf": [
                      {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "$ref": "#/$defs/pointPropertyPair",
                              "description": "List of X/Y property pairs to reproject as point coordinates."
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            },
                            {
                              "type": "object",
                              "required": [
                                "$ref"
                              ],
                              "properties": {
                                "x": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the X coordinate to reproject.",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Property name containing the X coordinate to reproject."
                                },
                                "y": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the Y coordinate to reproject.",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Property name containing the Y coordinate to reproject."
                                },
                                "crs": {
                                  "anyOf": [
                                    {
                                      "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`.",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                                },
                                "$ref": {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                }
                              },
                              "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                            }
                          ],
                          "description": "List of X/Y property pairs to reproject as point coordinates."
                        },
                        "description": "List of X/Y property pairs to reproject as point coordinates."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates."
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                  }
                },
                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
              }
            ],
            "description": "Value for each named entry in this registry."
          },
          "description": "Layer registry. At least one published layer is required. Each property name is the public layer identifier."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/$defs/layer",
                "description": "Value for each named entry in this registry."
              },
              {
                "type": "string",
                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
              },
              {
                "type": "object",
                "required": [
                  "$ref"
                ],
                "properties": {
                  "title": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable title shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable title shown in service metadata and clients."
                  },
                  "abstract": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Human-readable description shown in service metadata and clients."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Human-readable description shown in service metadata and clients."
                  },
                  "source": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Source name. Must reference a key from the top-level `sources` registry."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Source name. Must reference a key from the top-level `sources` registry."
                  },
                  "layer": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                  },
                  "dataset": {
                    "anyOf": [
                      {
                        "type": "string",
                        "minLength": 1,
                        "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
                  },
                  "crs": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                  },
                  "extent": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/bbox",
                        "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
                  },
                  "style": {
                    "anyOf": [
                      {
                        "type": "string",
                        "description": "Style name. Must reference a key from the top-level `styles` registry."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                        "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                      },
                      {
                        "type": "string",
                        "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                        "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                      }
                    ],
                    "description": "Style name. Must reference a key from the top-level `styles` registry."
                  },
                  "pointProperties": {
                    "anyOf": [
                      {
                        "type": "array",
                        "items": {
                          "anyOf": [
                            {
                              "$ref": "#/$defs/pointPropertyPair",
                              "description": "List of X/Y property pairs to reproject as point coordinates."
                            },
                            {
                              "type": "string",
                              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                            },
                            {
                              "type": "object",
                              "required": [
                                "$ref"
                              ],
                              "properties": {
                                "x": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the X coordinate to reproject.",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Property name containing the X coordinate to reproject."
                                },
                                "y": {
                                  "anyOf": [
                                    {
                                      "description": "Property name containing the Y coordinate to reproject.",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Property name containing the Y coordinate to reproject."
                                },
                                "crs": {
                                  "anyOf": [
                                    {
                                      "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`.",
                                      "type": "string"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                      "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                                    },
                                    {
                                      "type": "string",
                                      "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                      "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                                    }
                                  ],
                                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                                },
                                "$ref": {
                                  "type": "string",
                                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                                }
                              },
                              "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                            }
                          ],
                          "description": "List of X/Y property pairs to reproject as point coordinates."
                        },
                        "description": "List of X/Y property pairs to reproject as point coordinates."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates."
                  },
                  "$ref": {
                    "type": "string",
                    "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                    "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                  }
                },
                "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
              }
            ],
            "description": "Value for each named entry in this registry."
          }
        }
      ],
      "description": "Layer registry. At least one published layer is required. Each property name is the public layer identifier."
    },
    "layer": {
      "anyOf": [
        {
          "title": "Layer",
          "type": "object",
          "allOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/describedEntry",
                  "description": "Includes common descriptive fields: `title` and `abstract`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "object",
                  "required": [
                    "$ref"
                  ],
                  "properties": {
                    "title": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable title shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable title shown in service metadata and clients."
                    },
                    "abstract": {
                      "anyOf": [
                        {
                          "type": "string",
                          "description": "Human-readable description shown in service metadata and clients."
                        },
                        {
                          "type": "string",
                          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                        },
                        {
                          "type": "string",
                          "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                          "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                        },
                        {
                          "type": "string",
                          "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                          "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                        }
                      ],
                      "description": "Human-readable description shown in service metadata and clients."
                    },
                    "$ref": {
                      "type": "string",
                      "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                      "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                    }
                  },
                  "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                }
              ],
              "description": "Includes common descriptive fields: `title` and `abstract`."
            }
          ],
          "oneOf": [
            {
              "anyOf": [
                {
                  "required": [
                    "source",
                    "crs"
                  ],
                  "not": {
                    "anyOf": [
                      {
                        "required": [
                          "layer"
                        ]
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ]
                  },
                  "description": "First allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "First allowed object shape for this union."
            },
            {
              "anyOf": [
                {
                  "required": [
                    "layer"
                  ],
                  "not": {
                    "anyOf": [
                      {
                        "anyOf": [
                          {
                            "anyOf": [
                              {
                                "required": [
                                  "source"
                                ]
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              }
                            ]
                          },
                          {
                            "anyOf": [
                              {
                                "required": [
                                  "dataset"
                                ]
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              }
                            ]
                          }
                        ],
                        "description": "Alias layers must not define `source` or `dataset`; they point to another layer through `layer`."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      }
                    ],
                    "description": "Alias layers must not define `source` or `dataset`; they point to another layer through `layer`."
                  },
                  "description": "Second allowed object shape for this union."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Second allowed object shape for this union."
            }
          ],
          "properties": {
            "source": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Source name. Must reference a key from the top-level `sources` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source name. Must reference a key from the top-level `sources` registry."
            },
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
            },
            "dataset": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
            },
            "crs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
            },
            "extent": {
              "anyOf": [
                {
                  "$ref": "#/$defs/bbox",
                  "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry."
            },
            "pointProperties": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/pointPropertyPair",
                        "description": "List of X/Y property pairs to reproject as point coordinates."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "x": {
                            "anyOf": [
                              {
                                "description": "Property name containing the X coordinate to reproject.",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Property name containing the X coordinate to reproject."
                          },
                          "y": {
                            "anyOf": [
                              {
                                "description": "Property name containing the Y coordinate to reproject.",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Property name containing the Y coordinate to reproject."
                          },
                          "crs": {
                            "anyOf": [
                              {
                                "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`.",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                            "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                          }
                        },
                        "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates."
                  },
                  "description": "List of X/Y property pairs to reproject as point coordinates."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "List of X/Y property pairs to reproject as point coordinates."
            }
          },
          "unevaluatedProperties": false,
          "description": "Published layer definition. Either define a source-backed layer with `source` and `crs`, or alias another layer with `layer`."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "title": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable title shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable title shown in service metadata and clients."
            },
            "abstract": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Human-readable description shown in service metadata and clients."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Human-readable description shown in service metadata and clients."
            },
            "source": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Source name. Must reference a key from the top-level `sources` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Source name. Must reference a key from the top-level `sources` registry."
            },
            "layer": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Referenced layer name. In layer aliases, this must reference another top-level layer. In tilesets, this selects the included layer."
            },
            "dataset": {
              "anyOf": [
                {
                  "type": "string",
                  "minLength": 1,
                  "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Dataset name inside the selected source. Minimum length: 1. Minimum length: 1 character(s)."
            },
            "crs": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
            },
            "extent": {
              "anyOf": [
                {
                  "$ref": "#/$defs/bbox",
                  "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "Layer bounding box as `[minX, minY, maxX, maxY]` in the layer CRS. Exactly 4 numbers are required."
            },
            "style": {
              "anyOf": [
                {
                  "type": "string",
                  "description": "Style name. Must reference a key from the top-level `styles` registry."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Style name. Must reference a key from the top-level `styles` registry."
            },
            "pointProperties": {
              "anyOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/pointPropertyPair",
                        "description": "List of X/Y property pairs to reproject as point coordinates."
                      },
                      {
                        "type": "string",
                        "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                        "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                      },
                      {
                        "type": "object",
                        "required": [
                          "$ref"
                        ],
                        "properties": {
                          "x": {
                            "anyOf": [
                              {
                                "description": "Property name containing the X coordinate to reproject.",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Property name containing the X coordinate to reproject."
                          },
                          "y": {
                            "anyOf": [
                              {
                                "description": "Property name containing the Y coordinate to reproject.",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Property name containing the Y coordinate to reproject."
                          },
                          "crs": {
                            "anyOf": [
                              {
                                "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`.",
                                "type": "string"
                              },
                              {
                                "type": "string",
                                "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                                "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                              },
                              {
                                "type": "string",
                                "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                                "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                              },
                              {
                                "type": "string",
                                "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                                "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                              }
                            ],
                            "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
                          },
                          "$ref": {
                            "type": "string",
                            "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                            "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                          }
                        },
                        "description": "Objet de configuration fusionné avec une définition locale $defs via $ref"
                      }
                    ],
                    "description": "List of X/Y property pairs to reproject as point coordinates."
                  },
                  "description": "List of X/Y property pairs to reproject as point coordinates."
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                }
              ],
              "description": "List of X/Y property pairs to reproject as point coordinates."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "unevaluatedProperties": false
        }
      ],
      "description": "Published layer definition. Either define a source-backed layer with `source` and `crs`, or alias another layer with `layer`."
    },
    "pointPropertyPair": {
      "anyOf": [
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "x",
            "y"
          ],
          "properties": {
            "x": {
              "anyOf": [
                {
                  "description": "Property name containing the X coordinate to reproject.",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Property name containing the X coordinate to reproject."
            },
            "y": {
              "anyOf": [
                {
                  "description": "Property name containing the Y coordinate to reproject.",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Property name containing the Y coordinate to reproject."
            },
            "crs": {
              "anyOf": [
                {
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`.",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
            }
          },
          "description": "Pair of feature property names containing X/Y coordinates that should be reprojected along with the layer."
        },
        {
          "type": "string",
          "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
          "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
        },
        {
          "type": "object",
          "required": [
            "$ref"
          ],
          "properties": {
            "x": {
              "anyOf": [
                {
                  "description": "Property name containing the X coordinate to reproject.",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Property name containing the X coordinate to reproject."
            },
            "y": {
              "anyOf": [
                {
                  "description": "Property name containing the Y coordinate to reproject.",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Property name containing the Y coordinate to reproject."
            },
            "crs": {
              "anyOf": [
                {
                  "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`.",
                  "type": "string"
                },
                {
                  "type": "string",
                  "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
                  "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
                },
                {
                  "type": "string",
                  "pattern": "^\\$s\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}$",
                  "description": "Variable d’environnement chaîne : $s{VAR} ou $s{VAR|default}"
                },
                {
                  "type": "string",
                  "pattern": "\\$[sifb]\\{[A-Za-z_][A-Za-z0-9_]*(?:\\|[^}]*)?\\}",
                  "description": "Chaîne contenant une ou plusieurs variables d’environnement typées"
                }
              ],
              "description": "Coordinate reference system code for the layer or coordinate properties, for example `EPSG:4326`, `CRS:84`, `EPSG:3857`, `EPSG:2154`."
            },
            "$ref": {
              "type": "string",
              "pattern": "^#/\\$defs(?:/[^\\s]+)+$",
              "description": "Référence locale vers une définition de configuration, par exemple #/$defs/defaultTileset"
            }
          },
          "description": "Objet de configuration fusionné avec une définition locale $defs via $ref",
          "additionalProperties": false
        }
      ],
      "description": "Pair of feature property names containing X/Y coordinates that should be reprojected along with the layer."
    }
  }
}
